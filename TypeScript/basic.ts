//布尔类型
let isDone: boolean =false;
//数字
let decLiteral:number=6;
//字符串
// let name:string="bob";
// name="smith";

/**
 * 模板字符串
 * 它可以定义多行文本或内嵌表达式。
 * 这种字符串是被反引号包围（`）,并且以${expr}这种形式嵌入表达式
*/
let names: string = `Gene`;
let age:number=37;
let sentence:string=`Hello,my name is ${ names }.I'll be ${age+1} years old next month.`;


